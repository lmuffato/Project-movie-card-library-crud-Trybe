import React, { Component } from 'react';
import './Loading.css';

class Loading extends Component {
  render() {
    return (
      // <div>Loading message</div>
      <div className="loadingElement">Carregando...</div>
    );
  }
}

export default Loading;
